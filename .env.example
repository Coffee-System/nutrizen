# Exemplo de variáveis de ambiente para o projeto NutriZen
# Copie este arquivo para `.env.local` ou `.env` e ajuste os valores.

# Ambiente de execução
NODE_ENV=development

# URL pública do site (ajusta metadataBase)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Feature flags
NEXT_PUBLIC_ENABLE_EXPERIMENTAL_UI=false

# API base (quando backend existir)
NEXT_PUBLIC_API_BASE=https://api.nutrizen.dev

# Analytics (exemplos futuros)
# NEXT_PUBLIC_POSTHOG_KEY=
# NEXT_PUBLIC_POSTHOG_HOST=
