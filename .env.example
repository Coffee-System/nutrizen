# Example environment variables for the NutriZen project
# Copy this file to `.env.local` or `.env` and adjust the values.
# Do NOT commit real secrets in this file.

# Runtime environment
NODE_ENV=development

# Public site URL (affects metadataBase)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Feature flags
NEXT_PUBLIC_ENABLE_EXPERIMENTAL_UI=false

# API base (when backend is available)
NEXT_PUBLIC_API_BASE=https://api.nutrizen.dev

# Analytics / Observability (future)
# NEXT_PUBLIC_ANALYTICS_WRITE_KEY=
# NEXT_PUBLIC_SENTRY_DSN=

# Example for future AB testing or dark launches
# NEXT_PUBLIC_FEATURE_AI_MEAL_PLANNER=false

# Server-side only (would live in .env.local, not exposed)
# DATABASE_URL=postgres://user:pass@host:5432/nutrizen
# SUPABASE_SERVICE_ROLE_KEY=
# STRIPE_SECRET_KEY=

# IMPORTANT:
# - Variáveis começando com NEXT_PUBLIC_ são expostas no bundle do browser.
# - NÃO coloque segredos com este prefixo.
# - Validação ocorre em apps/web/env.ts (Zod schema).
