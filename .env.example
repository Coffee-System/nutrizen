# Example environment variables for the NutriZen project
# Copy this file to `.env.local` or `.env` and adjust the values.
# Do NOT commit real secrets in this file.

# Runtime environment
NODE_ENV=development

# Public site URL (affects metadataBase)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Feature flags
NEXT_PUBLIC_ENABLE_EXPERIMENTAL_UI=false

# API base (when backend is available)
NEXT_PUBLIC_API_BASE=https://api.nutrizen.dev

# Analytics (future examples)
# NEXT_PUBLIC_POSTHOG_KEY=
# NEXT_PUBLIC_POSTHOG_HOST=
